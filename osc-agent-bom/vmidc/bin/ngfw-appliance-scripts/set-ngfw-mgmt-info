#!/usr/bin/python

# Connects to the SMC for the first time.
# Usage: set-ngfw-mgmt-info fw-hostname smc-ip config-file-with-otp
# Just a wrapper

import sys
from sg.scripts.set_ngfw_mgmt_info import run
sys.exit(run())
