#!/usr/bin/python

# Usage: set-ngfw-root-password new-password
#        set-ngfw-root-password old-password new-password
# Sets the firewall root password
# old-password may be passed due to compatibility purposes with iSC, but it is not used.

# Calls the existing python script

import argparse
import os

parser = argparse.ArgumentParser(description='Set a firewall\'s root password for the iSC/NSX environment.')
parser.add_argument('old_password', nargs='?')
parser.add_argument('new_password')

args = parser.parse_args()

os.execl("/usr/lib/stonegate/bin/password-change", "password-change", "root", args.new_password)
